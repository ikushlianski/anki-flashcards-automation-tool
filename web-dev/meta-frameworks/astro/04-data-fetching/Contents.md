# Data Fetching

## What This Section Covers

**Fetch API**
- Using native `fetch()` in component frontmatter
- Top-level await for async data fetching
- Fetching from external APIs
- Error handling patterns

**Content Collections**
- Querying content with `getCollection()`
- Filtering and sorting collection entries
- Type-safe content queries
- Using `getEntry()` for single entries

**Local File Access**
- Using `import.meta.glob()` for batch file imports
- Eager vs lazy loading
- Working with markdown and JSON files

**Data Flow**
- Passing fetched data as props
- Sharing data between components
- Comparison with Next.js data fetching (`getServerSideProps`, `getStaticProps`)

**Caching and Performance**
- Build-time data fetching vs runtime
- When data is refetched
